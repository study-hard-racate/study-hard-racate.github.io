# DSA 可视化学习网站 — 优化完善计划书

> 编制时间：2026-08-19 | 当前版本：v2.0（40 个模块页面，第二阶段完成）
> 目标版本：v3.0（40+ 模块页面 + 交互增强 + 学习体验升级）
> 更新：2026-08-19 —— 第二阶段 15 个新模块全部完成（含 LCS/编辑距离/爬楼梯/LIS/Dijkstra/Prim/Kruskal/Floyd/计数/基数排序/汉诺塔/KMP/双向链表/循环队列/Trie），测试 383 全绿

---

## 一、项目现状

### 已有模块（40 个）

| 分类 | 模块 | 数量 |
|------|------|------|
| 排序 | 冒泡/选择/插入/快速/归并/希尔/堆/计数/基数排序 | 9 |
| 查找 | 线性/二分/哈希/分块 | 4 |
| 数据结构 | 数组/链表/双向链表/栈/队列/循环队列/Trie/并查集 | 8 |
| 树与图 | 二叉树/BST/遍历/堆/红黑树/BFS·DFS/拓扑/Dijkstra/Prim/Kruskal/Floyd | 11 |
| 动态规划 | 0-1背包/完全背包/LCS/编辑距离/爬楼梯/LIS | 6 |
| 经典算法 | 汉诺塔/KMP | 2 |

### 核心优势
- csim.js C 代码解释器（独一无二）
- 深色/浅色主题 + 移动端适配
- 键盘快捷键 + 速度控制 + 进度条拖拽 + 执行统计
- 全中文界面，面向新手
- 383 个 pytest 回归测试（csim 引擎 / 渲染器 / 全站结构）

---

## 二、优化目标

| 目标 | 具体指标 |
|------|----------|
| 模块数量 | 25 → 40+ |
| 交互深度 | 添加进度条、执行统计、算法对比 |
| 学习体验 | 添加学习路径、练习题、步骤详解 |
| 技术质量 | PWA 支持、性能优化、无障碍完善 |

---

## 三、分阶段实施计划

### 第一阶段：交互增强（2 周）

> 目标：提升现有模块的交互体验，不新增算法

#### 1.1 步骤进度条 ⏩
- **功能**：在播放器中添加可拖拽的进度条，支持跳转到任意步骤
- **改动文件**：`player.js`、`_player.html`（或对应模板）、`style.css`
- **实现方案**：
  - 添加 `<input type="range">` 作为进度条
  - 绑定 `oninput` 事件，调用 `player.goToStep(index)`
  - 在 `StepPlayer` 类中新增 `goToStep(i)` 方法
- **工期**：2 天

#### 1.2 执行统计面板 📊
- **功能**：在动画区域显示比较次数、交换次数、访问次数等
- **改动文件**：`demo.js`、`sorter.js`、`graph.js`、`style.css`
- **实现方案**：
  - 在 `step` 对象中累计统计信息
  - 在状态行下方显示统计卡片
  - 排序：比较次数 / 交换次数 / 归位次数
  - 查找：比较次数 / 访问次数
  - 图：访问顶点数 / 边数
- **工期**：3 天

#### 1.3 浅色主题 SVG 细节修复 🎨
- **功能**：修复 DP 箭头、哈希表标签等在浅色背景下对比度不足的问题
- **改动文件**：`style.css`（`body.light .anim-stage svg` 规则）
- **工期**：1 天

#### 1.4 `prefers-reduced-motion` 支持 ♿
- **功能**：尊重用户系统的"减少动画"设置
- **改动文件**：`style.css`
- **实现**：添加 `@media (prefers-reduced-motion: reduce)` 媒体查询
- **工期**：0.5 天

**阶段总工期：~6.5 天**

---

### 第二阶段：新算法模块（3 周）

> 目标：新增 15 个算法/数据结构模块

#### 2.1 动态规划扩展（4 个模块）

| 模块 | 难度 | 渲染器 | 说明 |
|------|------|--------|------|
| **LCS 最长公共子序列** | ⭐⭐⭐ | ✅ `renderDP2D` | ✅ 已完成（485aa5d） |
| **编辑距离** | ⭐⭐⭐ | ✅ `renderDP2D` | ✅ 已完成（485aa5d） |
| **最长递增子序列 LIS** | ⭐⭐⭐ | ✅ `renderLIS` | ✅ 已完成（6e157c0） |
| **爬楼梯** | ⭐ | ✅ `renderStairs` | ✅ 已完成（6e157c0） |

- **工期**：每个模块 2-3 天，共 ~10 天（✅ 全部完成）

#### 2.2 图算法扩展（4 个模块）

| 模块 | 难度 | 渲染器 | 说明 |
|------|------|--------|------|
| **Dijkstra 最短路径** | ⭐⭐⭐⭐ | ✅ `renderDijkstra` | ✅ 已完成（8ae16c2） |
| **Prim 最小生成树** | ⭐⭐⭐⭐ | ✅ `renderPrim` | ✅ 已完成（5fa960f）；key/inMST 数组模式 |
| **Kruskal 最小生成树** | ⭐⭐⭐ | ✅ `renderKruskal` | ✅ 已完成（1720c31）；边表 + 并查集判环 |
| **Floyd-Warshall 全源最短路** | ⭐⭐⭐ | ✅ `renderFloyd` | ✅ 已完成（5fa960f）；一维模拟距离矩阵 |

- **工期**：✅ 全部完成

#### 2.3 数据结构扩展（3 个模块）

| 模块 | 难度 | 渲染器 | 说明 |
|------|------|--------|------|
| **双向链表** | ⭐⭐ | ✅ `list.js` prevEdges | ✅ 已完成（104368b）；csim 增量收集 prev 字段 |
| **循环队列** | ⭐⭐ | ✅ `renderCircularQueue` | ✅ 已完成（104368b）；环形布局 |
| **Trie 字典树** | ⭐⭐⭐ | ✅ `renderTrie` | ✅ 已完成（c92958f）；数组模拟前缀树 |

- **工期**：✅ 全部完成

#### 2.4 排序扩展（2 个模块）

| 模块 | 难度 | 渲染器 | 说明 |
|------|------|--------|------|
| **计数排序** | ⭐ | ✅ `renderCountingSort` | ✅ 已完成（7279033）；三行视图 |
| **基数排序** | ⭐⭐ | ✅ `renderRadixSort` | ✅ 已完成（c265fec）；位数字标签 |

- **工期**：✅ 全部完成

#### 2.5 其他经典算法（2 个模块）

| 模块 | 难度 | 渲染器 | 说明 |
|------|------|--------|------|
| **KMP 字符串匹配** | ⭐⭐⭐ | ✅ `renderKMP` | ✅ 已完成（c265fec）；int 编码字符 + next 表 |
| **汉诺塔** | ⭐⭐ | ✅ `renderHanoi` | ✅ 已完成（7279033）；三柱盘片 |

- **工期**：✅ 全部完成

**第二阶段总进度：15/15 全部完成（2026-08-19）**

**阶段总工期：~39 天（约 6 周）**

---

### 第三阶段：交互升级（2 周）

> 目标：添加高级交互功能

#### 3.1 算法对比模式 🔄
- **功能**：两个算法并排播放，直观比较效率
- **改动文件**：新建 `compare.html` 模板、`style.css`
- **实现方案**：
  - 左右分屏，各加载一个算法
  - 同步播放/暂停
  - 底部显示统计对比（比较次数、交换次数）
- **工期**：4 天

#### 3.2 自定义代码编辑器 💻
- **功能**：用户可输入/编辑 C 代码，实时看到动画
- **改动文件**：新建 `playground.html`、集成 CodeMirror/Monaco 编辑器
- **实现方案**：
  - 添加代码编辑器（语法高亮 + 行号）
  - "运行"按钮调用 `CSim.run()`
  - 错误提示（行号 + 中文说明）
- **工期**：5 天
- **注意**：csim.js 已支持大部分 C 语法，只需前端集成

#### 3.3 步骤详解模式 📝
- **功能**：每个步骤旁边显示算法原理说明
- **改动文件**：`demo.js`、`generateStepComment` 函数
- **实现**：扩展步骤注释系统，添加"为什么这一步重要"的解释
- **工期**：3 天

**阶段总工期：~12 天**

---

### 第四阶段：学习体验（2 周）

> 目标：从"可视化工具"升级为"学习平台"

#### 4.1 学习路径 🗺️
- **功能**：推荐学习顺序，标记已学/未学
- **改动文件**：新建 `learning-path.html`、`style.css`
- **实现方案**：
  - 拓扑排序确定学习顺序
  - localStorage 记录学习进度
  - 每个模块显示"已学/未学"状态
- **工期**：3 天

#### 4.2 练习题系统 📝
- **功能**：每个模块配 2-3 道练习题
- **改动文件**：新建 `quiz.js`、各模块页面添加练习入口
- **实现方案**：
  - 选择题 + 填空题
  - 即时反馈 + 解析
  - localStorage 记录成绩
- **工期**：5 天

#### 4.3 算法复杂度对比图表 📈
- **功能**：首页展示所有算法的时间/空间复杂度对比
- **改动文件**：新建 `complexity-chart.js`、`index.html`
- **实现**：雷达图/柱状图对比
- **工期**：2 天

**阶段总工期：~10 天**

---

### 第五阶段：技术优化（1 周）

> 目标：提升性能、可维护性和可访问性

#### 5.1 PWA 支持 📱
- **功能**：离线缓存、添加到主屏幕
- **改动文件**：新建 `manifest.json`、`sw.js`（Service Worker）
- **工期**：2 天

#### 5.2 性能优化 ⚡
- **JS 按需加载**：每个页面只加载需要的 JS 模块
- **CSS 变量统一**：减少重复样式
- **SVG 渲染优化**：减少不必要的 DOM 操作
- **工期**：2 天

#### 5.3 英文版本 🌐
- **功能**：支持中英文切换
- **改动文件**：新建 `i18n.js`、提取所有硬编码中文
- **工期**：3 天

**阶段总工期：~7 天**

---

## 四、新模块详细设计

### 4.1 LCS 最长公共子序列

```c
// C 代码框架
int lcs(char* s1, char* s2, int m, int n) {
    int dp[m+1][n+1];
    for (int i = 0; i <= m; i++)
        for (int j = 0; j <= n; j++) {
            if (i == 0 || j == 0) dp[i][j] = 0;
            else if (s1[i-1] == s2[j-1]) dp[i][j] = dp[i-1][j-1] + 1;
            else dp[i][j] = max(dp[i-1][j], dp[i][j-1]);
        }
    return dp[m][n];
}
```

- **渲染**：扩展 `dp.js`，2D 表格 + 依赖箭头
- **数据规模**：两个字符串各 5-6 个字符
- **颜色**：蓝色=已填好，黄色=当前计算，紫色=依赖

### 4.2 Dijkstra 最短路径

```c
// C 代码框架
void dijkstra(int graph[N][N], int src) {
    int dist[N], visited[N];
    for (int i = 0; i < N; i++) { dist[i] = INF; visited[i] = 0; }
    dist[src] = 0;
    for (int count = 0; count < N-1; count++) {
        int u = minDistance(dist, visited);
        visited[u] = 1;
        for (int v = 0; v < N; v++)
            if (!visited[v] && graph[u][v] && dist[u] != INF
                && dist[u] + graph[u][v] < dist[v])
                dist[v] = dist[u] + graph[u][v];
    }
}
```

- **渲染**：扩展 `graph.js`，圆形布局 + 距离标签 + 已确定顶点绿色
- **数据规模**：6 个顶点
- **颜色**：黄色=当前处理，绿色=已确定最短路，蓝色=待处理

### 4.3 编辑距离

```c
// C 代码框架
int editDistance(char* s1, char* s2, int m, int n) {
    int dp[m+1][n+1];
    for (int i = 0; i <= m; i++)
        for (int j = 0; j <= n; j++) {
            if (i == 0) dp[i][j] = j;
            else if (j == 0) dp[i][j] = i;
            else if (s1[i-1] == s2[j-1]) dp[i][j] = dp[i-1][j-1];
            else dp[i][j] = 1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]);
        }
    return dp[m][n];
}
```

- **渲染**：扩展 `dp.js`，2D 表格 + 三种操作标记
- **数据规模**：两个字符串各 4-5 个字符
- **颜色**：绿色=匹配，黄色=当前计算，红色=操作（插入/删除/替换）

---

## 五、资源需求

### 5.1 开发时间估算

| 阶段 | 工期 | 优先级 |
|------|------|--------|
| 第一阶段：交互增强 | 2 周 | 🔴 高 |
| 第二阶段：新算法模块 | 6 周 | 🔴 高 |
| 第三阶段：交互升级 | 2 周 | 🟡 中 |
| 第四阶段：学习体验 | 2 周 | 🟡 中 |
| 第五阶段：技术优化 | 1 周 | 🟢 低 |
| **总计** | **13 周** | |

### 5.2 技术栈（无需新增）

- 前端：原生 JS + SVG + CSS（无框架）
- 后端：无（纯静态站）
- 部署：GitHub Pages
- 测试：pytest（现有 159 个）

---

## 六、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| csim.js 不支持新算法语法 | 新模块无法实现 | 优先选择 csim 已支持的语法子集 |
| SVG 渲染性能下降 | 动画卡顿 | 限制数据规模，优化渲染逻辑 |
| 测试回归 | 现有功能破坏 | 每次修改后运行全量测试 |
| 移动端兼容性 | 部分功能不可用 | 渐进增强，移动端降级 |

---

## 七、成功标准

### v1.5（第一阶段完成后）
- [x] 进度条可拖拽跳转
- [x] 执行统计面板显示
- [x] 浅色主题 SVG 修复
- [x] 所有现有测试通过（280 个，已移植到本仓库）

### v2.0（第二阶段完成后）
- [x] 模块数量 ≥ 35（当前 40 ✅）
- [x] 新增模块均有复杂度卡片
- [x] 新增模块均有步骤注释
- [x] 所有新模块测试通过（专项测试 15 组，全量 383）

### v3.0（全部完成后）
- [ ] 模块数量 ≥ 40
- [ ] 算法对比模式可用
- [ ] 自定义代码编辑器可用
- [ ] 学习路径 + 练习题可用
- [ ] PWA 支持离线使用

---

## 八、优先级排序（推荐执行顺序）

如果时间有限，按以下优先级实施：

1. ⏩ **步骤进度条**（✅ 已完成）
2. 📊 **执行统计面板**（✅ 已完成）
3. 📝 **LCS / 编辑距离 / 爬楼梯 / LIS**（✅ 已完成）
4. 🗺️ **Dijkstra / Prim / Kruskal / Floyd**（✅ 已完成）
5. 🧩 **汉诺塔 / KMP / Trie / 双向链表 / 循环队列 / 计数 / 基数排序**（✅ 已完成）
6. 💻 **自定义代码编辑器**（从"可视化"升级为"实验平台"）
7. 🔄 **算法对比模式**（差异化竞争力）
8. 📱 **PWA 支持**（移动端体验）
9. 🌐 **英文版本**（国际化）
